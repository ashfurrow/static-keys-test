# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

plugin 'cocoapods-keys', {
  :project => "Test",
  :keys => [
    "key"
  ]
}

target 'Test' do

pod 'AFNetworking'

end

target 'TestTests' do

end

